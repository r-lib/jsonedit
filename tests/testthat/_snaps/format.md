# formatting retains comments

    Code
      cat(text_format(text))
    Output
      {
          // a comment
          "a": 1, // another one
          "b": {
              "c": 2
          }
      } // trailing

